name: Validate CodeForge Core

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'src/vs/workbench/contrib/codeforge/**'
      - 'src/vs/workbench/browser/workbench.zenMode.contribution.ts'
      - '.github/workflows/validate-codeforge-core.yml'

permissions:
  contents: read

jobs:
  typecheck-core:
    name: Type-check CodeForge workbench core
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout CodeForge
        uses: actions/checkout@v4
        with:
          lfs: false

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: npm

      - name: Install TypeScript dependencies
        run: npm ci --ignore-scripts

      - name: Type-check focused CodeForge workbench core
        run: npx tsc --project src/vs/workbench/contrib/codeforge/tsconfig.json --noEmit
